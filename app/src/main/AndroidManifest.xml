<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_projectlogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_projectlogo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project_management_G1"
        tools:targetApi="31">
        <activity
            android:name=".UI.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".UI.GanttChartActivity"
            android:exported="false">

        </activity>

        <service android:name=".UI.BackgroundMusicService" android:enabled="true"
            android:exported="false" />
    </application>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>